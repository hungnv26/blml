# Pinned: this container is rebuilt on the VPS, and an unpinned build that
# works today and breaks in six months is worse than a stale dependency.
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
psycopg2-binary==2.9.10
websocket-client==1.8.0
